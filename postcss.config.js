module.exports = {
  plugins: {
    // Estas dependencias deben estar listadas en package.json (dev-dependencies)
    // para que Tailwind CSS funcione correctamente en el proceso de build de Vite.
    'tailwindcss': {},
    'autoprefixer': {},
  },
};
